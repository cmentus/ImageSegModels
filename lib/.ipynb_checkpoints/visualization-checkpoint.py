def vis_chest_xray(model,img,truth):
	''' Plot each layer of the prediction +truth in separate panes. '''
	return
def vis_attention(model,img,truth,args):
	''' Plot the attention heads from the depth provided in args'''
	return